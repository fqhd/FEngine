#ifndef MENU_H
#define MENU_H

#include "Settings.hpp"

class Menu {
public:

     void init(Settings& settings);
     void update();
     void render();
     void destroy();


private:



};

#endif
